trigger ZK_LeadTrigger on Lead (before insert, before update, before delete, after insert, after update, after delete, after undelete) {
    //ZK_LeadTriggerHandler handler = new ZK_LeadTriggerHandler();
    //handler.execute();
}